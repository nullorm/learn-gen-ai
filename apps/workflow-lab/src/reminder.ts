// TODO (Module 15, Section 5): implement reminderWorkflow.
// - record 'created' (a `'use step'`), then `await sleep('1 hour')`, then record 'reminded';
//   return 'done'. Import sleep from 'workflow'.
export async function reminderWorkflow(): Promise<string> {
  'use workflow'
  throw new Error('TODO: implement reminderWorkflow (Module 15, Section 5)')
}
